node {
  
  stage('Checking out code') {
	checkout scm
}
  stage('Building app') {
	sh 'echo "building app"'
} 

  stage('starting app') {
	sh 'npm --version'
        sh 'node --version'
}
  
 
} // end
